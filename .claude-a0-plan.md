# A0 CI verification — working plan (keep updated at every significant step)

## Standing constraints
- NEVER touch/comment/create visible activity on real upstream `esnet/esnet-matrix-panel` PRs.
  All experimentation stays in fork `zedRus25/esnet-matrix-panel`.
- This file lives at repo root temporarily as a crash-safe memory aid across autocompaction.
  Delete it once A0 is confirmed green and reported to the user.

## Goal
Get PR #1 (zedRus25/esnet-matrix-panel, branch `upstream-clean-base` -> `upstream-master-mirror`)
fully green: build/lint/typecheck, full playwright-tests matrix, and pr-screenshot-diff producing
a real `before-after-screenshots` artifact. Then report run URL + artifact list to user.
Then (separate, NOT part of A0 diff): add Playwright/Chromium + Docker image caching as follow-up.

## Branches
- `upstream-clean-base` (local + origin): where all A0 fix commits live.
- `upstream-master-mirror` (origin only, pinned to real-upstream master tip 6fd187f): PR #1's base,
  created purely so `git merge-base` is meaningful (fork's own `master` was an unrelated old bun rewrite).
- `claude/esnet-matrix-upstream-sync-f4p5k7`: THIS session's designated push branch (unrelated docs/typecheck
  work, not where A0 lives). Return here between CI check-ins.
- `main` local branch: also exists, not primary focus.

## Commit chain on upstream-clean-base (oldest -> newest), all fixing CI issues found via draft PR #1:
1. 43b4562 docs: add A0 plan
2. 7555159 docs: add Phase A PR plans
3. cce8168 docs: settle A0 design decisions
4. 3e4e4ef feat: add e2e smoke tests + before/after screenshot CI job (A0 core diff)
5. ed24a0c fix: unblock strict typecheck in dataParser.ts (pre-existing bug, unrelated to A0)
6. a8ae2bc fix: assert non-null on Field.display/DisplayValue.color too
7. 7908d16 fix: exclude vendored src/d3.min.js from eslint
8. fb49483 fix: reinstall deps after each checkout in pr-screenshot-diff job
9. e0dff07 fix: target chart svg by id (#svg-N) instead of ambiguous generic svg selector
10. e0e12a1 fix: drop --frozen-lockfile for per-checkout reinstalls in pr-screenshot-diff
11. 7ee478d fix: --ignore-engines for per-checkout reinstalls in pr-screenshot-diff
12. ee2233a fix: restore new e2e specs (tests/) onto merge-base checkout; retarget PR base to upstream-master-mirror
13. ece557f ci: add upstream-master-mirror to pull_request.branches trigger (fork-experimentation-only)

## CI run history (zedRus25/esnet-matrix-panel, workflow ci.yml, PR #1)
| run # | sha | result | notes |
|---|---|---|---|
| 1 | 3e4e4ef | failure | typecheck pre-existing bug |
| 2 | ed24a0c | failure | more typecheck (Field.display) |
| 3 | a8ae2bc | failure | eslint on vendored d3.min.js |
| 4 | 7908d16 | failure | pr-screenshot-diff TS5095 (stale node_modules across checkouts) |
| 5 | fb49483 | failure | playwright strict-mode svg selector ambiguity |
| 6 | e0dff07 | failure | --frozen-lockfile fails on ancient merge-base lockfile |
| 7 | e0e12a1 | failure | --ignore-engines needed (eslint-plugin-jsdoc node engine) |
| 8 | 7ee478d | failure | root cause found: PR based on unrelated fork master; no spec in "before" tree |
| 9 | ece557f | **SUCCESS (all jobs except publish-report)** | trigger-branch fix — this is the green run |
| 10 | a421b5e | pending | fix duplicate Authorization header in publish-report (persist-credentials: false) |

## Current status — A0 CI VERIFICATION: DONE ✅
- Run #9 (id 32574316744, sha ece557f) status: completed.
  https://github.com/zedRus25/esnet-matrix-panel/actions/runs/32574316744
- Build, lint and unit tests: success. Resolve e2e images: success.
- Before/after panel screenshots (pr-screenshot-diff): success — FIRST time ever green,
  including "Capture before/after screenshots", "Run smoke specs (before/after)", "Upload before/after screenshots".
- All 6 e2e matrix jobs (grafana-enterprise 12.4.9/13.2.0/12.3.11/13.1.4/nightly/13.0.7): success.
- publish-report: failure — EXPECTED/OUT OF SCOPE (no gh-pages branch on fork). Not actionable.
- Confirmed via list_workflow_run_artifacts: 14 artifacts present, including the previously-never-seen
  `before-after-screenshots` (id 9476172001, 54522 bytes). Full list recorded in conversation; key ones:
  before-after-screenshots, 6x smoke-screenshots-grafana-enterprise-*, 6x gf-playwright-report-grafana-enterprise-*,
  esnet-matrix-panel-2.1.0 (plugin zip, 1214486 bytes).
- Reported run URL + artifact list to user: DONE.

## publish-report root cause (found and fixed, commit a421b5e)
NOT a missing secret/permission. Job already had contents:write + secrets.GITHUB_TOKEN.
Real cause: outer `actions/checkout@v6` had `persist-credentials: true`, leaving a git
extraheader credential in place. The `grafana/plugin-actions/playwright-gh-pages/deploy-report-pages`
composite action then injects its OWN Authorization header via its `github-token` input during its
internal checkout of gh-pages -> two Authorization headers -> GitHub API 400
(`remote: Duplicate header: "Authorization"`). Fix: set `persist-credentials: false` on the outer
checkout so only the composite action's own auth applies. Pushed as commit a421b5e on upstream-clean-base.
Still need to confirm: (a) run for a421b5e goes green on publish-report, (b) peaceiris/actions-gh-pages
(used internally) successfully creates the gh-pages branch on first run since it doesn't exist yet on
the fork — should auto-create via orphan branch push, but verify in the logs once it runs.

## GitHub Pages settings answer (user asked, answered)
User asked whether to enable Pages via "GitHub Actions" source or point it at gh-pages branch.
Answer: use "Deploy from a branch" -> gh-pages (NOT the "GitHub Actions" build-and-deploy source).
Reason: deploy-report-pages uses peaceiris/actions-gh-pages internally, which pushes static report
HTML directly to a gh-pages branch -- it does NOT use the actions/deploy-pages + upload-pages-artifact
OIDC flow that the "GitHub Actions" Pages source expects. So Settings -> Pages -> Source must be
"Deploy from a branch", branch gh-pages, folder /(root). gh-pages branch must exist first (created by
this job's push) before that dropdown option will even find it.

## Housekeeping
- Committed this plan file itself as commit 35baa24 (docs: track A0 verification plan file for
  crash-safety) after a stop-hook flagged it as untracked. Pushed to origin/upstream-clean-base.

## FULLY GREEN — run 32574646047, commit a421b5e (confirmed 2026-08-22T13:05:54Z)
All 10 jobs succeeded: build, resolve-versions, all 6 e2e matrix jobs, pr-screenshot-diff
(before/after), AND publish-report (job id 97035401417, conclusion success). This is the
first time publish-report has ever gone green. A0 CI verification is now 100% complete —
no more outstanding CI failures anywhere in the pipeline.
https://github.com/zedRus25/esnet-matrix-panel/actions/runs/32574646047
gh-pages branch confirmed created on fork (sha da36917). User is setting Settings -> Pages ->
Source -> Deploy from a branch -> gh-pages -> / manually (their action, not ours).

## Report URL structure (confirmed via get_file_contents on gh-pages branch)
NO root index.html -- https://zedrus25.github.io/ (user Pages site, needs a zedrus25.github.io repo,
doesn't exist) and bare https://zedrus25.github.io/esnet-matrix-panel/ will BOTH 404 (user hit the
former). Content is nested: <date>/<run-number>/grafana-enterprise-<version>/index.html, e.g.:
https://zedrus25.github.io/esnet-matrix-panel/20260822/1/grafana-enterprise-12.4.9/
(6 sibling folders under 20260822/1/, one per matrix version: 12.3.11, 12.4.9, 13.0.7, 13.1.4,
13.2.0, nightly). User needs Pages enabled first (branch gh-pages, root) AND to browse into one
of these version subfolders, not the bare repo Pages root.

## Next steps
1. [DONE] A0 CI verification reported to user (run 32574316744 fully green except publish-report,
   then run 32574646047 fully green INCLUDING publish-report).
2. [DONE] publish-report fix confirmed working; gh-pages branch created.
3. [DONE] User enabled Pages: Settings -> Pages -> Source: Deploy from a branch -> gh-pages -> /(root).
4. [DONE] User reported bare https://zedrus25.github.io/ 404s (wrong URL scope + no root index.html on
   gh-pages) -> user asked for a root index.html listing runs + tabular before/after comparison.
5. [IN PROGRESS] Implemented on upstream-clean-base (not yet pushed/verified via CI):
   a. playwright-tests: `upload-report: false` -> `true` in "Upload e2e test summary" step, so
      per-version gh-pages folders get a real Playwright HTML report instead of only summary.txt.
   b. pr-screenshot-diff: new "Generate before/after comparison page" step (after Save "after"
      screenshots, before Upload before/after screenshots) writes a self-contained
      `before-after/index.html` with a table pairing default-panel.png/grouped-panel.png via
      relative <img src="before/...">/<img src="after/..."> paths. Lives inside the existing
      before-after-screenshots artifact too (user wanted both artifact + gh-pages).
   c. publish-report: added `pr-screenshot-diff` to `needs:` (safe with existing `if: always()`,
      since pr-screenshot-diff is skipped on non-PR events). Added 3 new steps after the existing
      "Publish report" step:
      - Download before/after screenshots artifact (if: pull_request event only, continue-on-error
        since it won't exist on push-triggered runs).
      - Checkout gh-pages branch into ./gh-pages (persist-credentials: true -- safe here since this
        is a separate checkout step, not colliding with deploy-report-pages' own internal checkout
        which already completed in the prior step).
      - "Publish before/after comparison and rebuild index" step: copies before-after/ content to
        gh-pages/<YYYYMMDD>/<run-number>/before-after/ (same date/run-number convention as the
        existing grafana-enterprise-<version>/ dirs), then regenerates root index.html by scanning
        all <date>/<run>/<report-or-before-after> dirs on gh-pages and linking to each, then commits
        (github-actions[bot] identity) and pushes to gh-pages only if there's a diff.
   Root cause validated earlier: gh-pages per-version dirs only had summary.txt (UPLOAD_REPORT_ENABLED=false)
   and no root index.html existed at all -- both addressed by the above.
6. [PUSHED, CI RUNNING] Pushed as commit fb88b24 on upstream-clean-base. Triggered CI run
   32574185072/32575185072 (run #14, PR #1). As of last check: build/lint/unit tests + resolve-versions
   green; 6x e2e matrix jobs + pr-screenshot-diff still in_progress. Need to verify once complete:
   - upload-report:true actually produces full HTML reports (not just summary.txt) per version.
   - before-after/index.html renders correctly in the downloaded artifact (relative img paths resolve).
   - gh-pages gets a new <date>/<run>/before-after/ dir with the comparison page.
   - root index.html appears on gh-pages, lists all dates/runs, links resolve, before-after link included.
   - Watch for edge cases: date-dir mismatch between deploy-report-pages' internal date-stamping and
     this new step's `date -u +%Y%m%d` (should match since same job run, but confirm in logs); git push
     to gh-pages needing contents:write permission (already granted at job level).
   A background wakeup is scheduled to re-check this run and follow up (fix-and-repush if it fails,
   or confirm+report if green).
7. [NOT STARTED] Once green, report new root gh-pages URL (https://zedrus25.github.io/esnet-matrix-panel/)
   to user and confirm before/after tabular comparison visible both in artifact and on gh-pages.

## Follow-up: Playwright/Chromium + Grafana docker image caching (started, same branch)
User asked to add this now. Implemented directly in .github/workflows/ci.yml (not yet committed as of
writing this section -- see Housekeeping below for commit status):
- `playwright-tests` job (per e2e matrix version):
  - "Cache Playwright browsers": actions/cache@v4 on `~/.cache/ms-playwright`, keyed by
    `${{ runner.os }}-playwright-${{ hashFiles('yarn.lock') }}` (bumps automatically on yarn.lock changes,
    which is what pins the playwright version). Placed right before "Install Playwright Browsers".
  - "Cache Grafana base image" / "Load cached Grafana base image" / "Export Grafana base image for cache":
    actions/cache@v4 on a saved `docker save` tarball at /tmp/docker-cache/grafana-image.tar, keyed by
    `grafana-base-image-<NAME>-<VERSION>`. Skipped entirely for the `nightly` matrix entry (moving tag --
    caching it would pin a stale nightly build indefinitely). On cache hit, `docker load`s the tar before
    "Start Grafana" instead of re-pulling; on miss, `docker save`s the base image (`grafana/<NAME>:<VERSION>`,
    the Dockerfile's FROM target) right after Grafana successfully starts, so actions/cache's post-job step
    persists it for next time.
- `pr-screenshot-diff` job (single pinned version, GRAFANA_VERSION=12.4.0): same two cache patterns added
  (Playwright browsers cache before "Install Playwright Browsers"; Grafana base image cache/load before
  "Resolve merge base", export right after "Wait for grafana server (before)" -- the "before" and "after"
  builds within this one job already share Docker's local image cache once loaded/pulled once, so caching
  is only needed for the first pull/build of the job).
- Rationale: these were the two heaviest, most repetitive steps across every job/matrix entry (installing
  Chromium+deps fresh every run, and pulling/building large Grafana docker images fresh every run) that
  don't otherwise benefit from yarn's own dependency cache (`cache: yarn` on actions/setup-node only
  covers node_modules, not the separately-downloaded Playwright browser binaries or docker images).
- [PUSHED] Committed and pushed to upstream-clean-base alongside this plan-file update. First run on this
  commit will be cold-cache by definition; need a *second* run to confirm actual cache hits/runtime win.

### REGRESSION found in commit b26141a (this caching commit) — run 32575578468, FAILED (6 jobs)
Root cause (single cause, explained both failure symptoms):
- `docker compose` has no top-level `image:` for the `grafana` service (it's build-only, base image only
  referenced inside the Dockerfile's `FROM grafana/${grafana_image}:${grafana_version}`). So
  `docker compose pull` is a no-op for it, and buildkit does NOT leave that FROM base image tagged in the
  classic `docker images` store after `docker compose up -d --build` -- confirmed via job log
  ("naming to docker.io/library/esnet-matrix-panel-grafana" -- only the FINAL built image gets a local tag).
  So `docker save grafana/<image>:<version>` failed with `Error response from daemon: reference does not
  exist` in BOTH jobs that do it (pr-screenshot-diff, and every playwright-tests matrix entry).
- That `docker save` step had no `if: always()`/`continue-on-error`, so its failure ABORTED the job before
  "Run Playwright tests" ever ran (test-outcome ended up literally "skipped"), which is why the
  *pre-existing* "Upload e2e test summary" step then failed too, with `PARENT_DIR` empty (its value comes
  from a `mv`-based composite-action step keyed off the (never-created) playwright-report dir, which never
  ran) -- this was NOT a separate regression, just a downstream symptom of the same docker save failure.
- **Fix (commit d207806, pushed)**: added an explicit `docker pull grafana/<image>:<version>` step (guarded
  by `cache-hit != 'true'`) right before the existing "Export Grafana base image for cache" step in BOTH
  jobs, so the tag actually exists locally before `docker save` runs. Moved pr-screenshot-diff's
  pull+export earlier (right after "Load cached Grafana base image", before the first `docker compose up`)
  since GRAFANA_IMAGE/VERSION are fixed env vars there, not matrix-dependent, so there's no need to wait for
  "Wait for grafana server (before)". Also made the export step `continue-on-error: true` in both jobs --
  caching is an optimization only, it must never be able to fail the actual e2e run again.
- [PUSHED as d207806] Also (user's follow-up ask, same commit): added a "Link report in job summary" step
  at the end of `publish-report`, echoing the gh-pages report index URL and this run's specific report URL
  into the GitHub Actions job summary (`$GITHUB_STEP_SUMMARY`) -- computed from `$GITHUB_REPOSITORY_OWNER`/
  `$GITHUB_REPOSITORY` env vars, no PR comment posted (no upstream-visible noise).
- **STATUS: CONFIRMED FULLY GREEN.** Run 32576015909 (commit ca7c257, on top of fix commit d207806),
  status completed / conclusion success. All jobs including pr-screenshot-diff and publish-report
  succeeded. https://github.com/zedRus25/esnet-matrix-panel/actions/runs/32576015909
  A0 + caching follow-up work is now fully done and verified green. Moving on to PR A1 (fitToPanel)
  on a new branch `upstream-a1-fit-to-panel` off `upstream-clean-base`, per
  doc/plans/260822-split_pr-A1-fit-to-panel.md.

## User-reported 404 (resolved as user error, not a bug)
User visited https://zedrus25.github.io/20260822/14/before-after and got 404 despite content existing.
Root cause: this is a *project* Pages site (repo is not <user>.github.io), so all paths must be prefixed
with /esnet-matrix-panel/. Verified via get_file_contents on gh-pages branch that
20260822/14/before-after/index.html (and before/, after/ subdirs) all exist correctly, and root
index.html also exists and looks right. Correct URL:
https://zedrus25.github.io/esnet-matrix-panel/20260822/14/before-after/ (note trailing slash).
User's URL had neither the repo prefix nor the trailing slash. No code change needed -- told
user the correct URL.
8. [NOT STARTED, LOWER PRIORITY, SEPARATE CHANGE] Deferred follow-up — add actions/cache for
   Playwright/Chromium browser binaries (and consider docker layer/image caching) as a SEPARATE
   change on top of upstream-clean-base, not folded into this diff or A0's diff.
9. Keep this file updated at each significant step (per user's explicit standing instruction).
10. Throughout: never touch real upstream repo; stay on claude/esnet-matrix-upstream-sync-f4p5k7 as the
    session's own push branch when not actively working on upstream-clean-base.

## B0 (broaden CI test dashboard/e2e coverage) — IN PROGRESS, on this branch (upstream-clean-base)
User request: "we should probably merge b0 into a0 and rerun the a* prs with the full test suite,
before diving into b1-5." Spec: doc/plans/260822-split_pr-B0-broaden-ci-test-dashboards.md (lives on
branch upstream-a2-extra-tooltip-fields, not here -- read via git show).

Plan:
1. [DONE] Added 11 new panels (ids 3-13) to provisioning/dashboards/dashboard.json, each exercising a
   previously-uncovered MatrixOptions code path: static row/col headings, null vs no-data cells,
   categorical legend, data-link URL, col-grouping-only, row-grouping-only, sortType natural-desc,
   sortType none + both groupings (unsorted categories map path), threshold-based coloring
   (color.mode: thresholds), long-label truncation (>50 chars, default txtLength), and CSV data shape
   (scenarioId: csv_content). Deliberately SKIPPED plan items 10/11 (gated on A2 extraTooltipFields /
   A1 fitToPanel merging) per the plan doc's own contingency clause -- neither A1 (#2) nor A2 (#4) is
   merged yet.
2. [DONE] Added matching Playwright tests (one per new panel, ids 3-13) to tests/matrix-panel.spec.ts,
   following the exact existing pattern (getPanelById -> visible/no-error-icon/#svg-N visible ->
   screenshot to test-results/screenshots/<name>.png).
3. [NEXT] Commit both files + this plan update, push to origin/upstream-clean-base, then drive the
   resulting PR #1 CI run to fully green via direct get_check_runs polling (never trust a single
   webhook). Real risk: 10 of 11 panels are entirely new option combos never exercised in CI before
   (csv_content scenario, color.mode=thresholds, sortType=none, standalone grouping flags) --
   expect possible failures needing fixes to dashboard.json/spec file.
4. [NOT STARTED] Once B0 green on upstream-clean-base/PR#1: rebase upstream-a1-fit-to-panel (#2),
   upstream-a2-extra-tooltip-fields (#4), upstream-a3-bundle-trim (#3), upstream-a4-pixel-truncation (#5)
   onto the new upstream-clean-base tip, force-push each (own unmerged branches, safe), and re-verify
   each PR's CI goes green against the broadened suite via direct get_check_runs polling.
5. [NOT STARTED] Only after both 3 and 4 are done: consider starting B1-B5 (still planning-only docs).
6. Standing reminders still apply: never touch/comment on real upstream esnet/esnet-matrix-panel;
   keep this file updated at each significant step (note: NOT unified across branches -- each branch's
   copy is independent from whenever it branched).

### B0 CI debugging: two distinct bugs found and fixed
Initial B0 push (87f1a0d) had 7/13 e2e tests failing (panels 5,7,8,9,11,12,13). Local repro blocked by
sandbox network policy, so diagnosed entirely via CI job log analysis (temporary debug instrumentation
in tests/matrix-panel.spec.ts: pageerror/console-error listeners, later a DOM-dump afterEach hook --
both since removed).

**Bug 1 (panel 5): `src/matrix.js` line 593 -- FIXED in commit 5f33145.**
The categorical-legend branch of the legend-rendering code did `svg.append('g').selectAll('legendLabels')`
but `svg` was never defined in that scope -- the correct variable, already used by the sibling `range`-type
legend branch, is `svgLegend` (declared line 572: `const svgLegend = d3.select(\`#${legendClass}\`);`).
This threw `ReferenceError: svg is not defined` on EVERY page load (confirmed via 27 occurrences in one
job's log, across passing and failing tests alike, since panel 5's categorical legend is on the shared
13-panel dashboard loaded by every test). Fix: `svg` -> `svgLegend` on that line. Confirmed via next CI
run: 0 occurrences of `ReferenceError` in the log, and pass count went from 6->8 (panel 5 now passes).

**Bug 2 (panels 7,8,9,11,12,13): NOT a code bug -- test-infrastructure gap. FIXED in commit b56a041.**
Root cause: default Chromium viewport (1280x720, no override in playwright.config.ts) doesn't fit the
now-4-row-tall B0 dashboard (gridPos rows at y=0,15,30,45). Grafana lazy-mounts/virtualizes panels
outside the current viewport -- they don't exist as `[data-viz-panel-key]` elements in the DOM at all
until scrolled into view. Confirmed via a DOM-dump `afterEach` hook that queried every
`[data-viz-panel-key]` element on any non-passing test and logged its innerHTML: across the ENTIRE job
log (14 dump groups), the ONLY panel keys that ever appeared were panel-1,2,3,4,5,6,10 -- panels
7,8,9,11,12,13 never appeared even once, proving they were never mounted (not: mounted-but-broken).
This also explains why the first round of instrumentation (pageerror/console-error listeners) captured
nothing for these 6 panels -- there was no exception or error to capture, since Grafana simply never
rendered them into the DOM.
Fix: added `await panel.locator.scrollIntoViewIfNeeded();` immediately after `getPanelById(...)` and
before any assertion, in all 13 tests -- brings the target panel into the viewport so Grafana mounts it
before the visibility/svg checks run. Also removed both rounds of debug instrumentation now that root
causes are confirmed (they were explicitly temporary).
**UPDATE: the scrollIntoViewIfNeeded() fix in b56a041 did NOT work.** CI for that commit came back
8 passed / 6 failed -- same exact 6 panels (7,8,9,11,12,13) still failing, but now with a different
error: `locator.scrollIntoViewIfNeeded: Test timeout of 30000ms exceeded ... waiting for locator
('[data-viz-panel-key="panel-N"]')`. Root issue: Playwright locator actions (including
scrollIntoViewIfNeeded) auto-wait for the target element to attach to the DOM first -- but Grafana
never attaches these panels until they're already within the viewport. That's a chicken-and-egg
deadlock: you can't scroll an element into view if the act of finding it to scroll requires it to
already be attached, and it only attaches once already in view. So scrolling the specific target
locator can never work here.

**Actual fix (commit 4d92bd9): use a tall Playwright viewport so nothing needs scrolling.**
Added `viewport: { width: 1280, height: 2400 }` to `use` in `playwright.config.ts`. The dashboard's
panels span grid rows y=0 to y=60 (see `gridPos` in dashboard.json); 2400px comfortably fits the
whole thing plus Grafana chrome (nav/header/time-picker), so every panel mounts immediately on page
load with no scrolling needed at all. Left the (now redundant but harmless no-op)
`scrollIntoViewIfNeeded()` calls from b56a041 in place in the test file to minimize diff churn --
they resolve instantly since every panel is already in view.

**STATUS as of commit 4d92bd9: viewport fix was only PARTIAL. Superseded below.**
CI for 4d92bd9 came back version-dependent: the 6-way Grafana-version matrix job had 3/6 versions
green (13.1.4, 13.2.0, nightly -- all 13/13 passed) and 3/6 red (12.3.11, 12.4.9, 13.0.7 -- each
failing the SAME 6 panels every time: 7,8,9,11,12,13, via `scrollIntoViewIfNeeded` 30s timeouts,
i.e. never mounted). Initial read: "version-specific behavioral difference in older Grafana."

**That reading was WRONG -- overturned by the separate "Before/after panel screenshots" job.**
That job pins a single Grafana version (12.4.0) in its own container/run, so if the bug were purely
version-tied it should show the same ~6-panel failure signature. Instead it failed 11 of 13 panels
(only panels 1 and 2 passed) -- a much larger and DIFFERENT failing set than the matrix job's 12.x
runs (it additionally failed panels 3,4,5,6,10, which pass in the matrix job). A fixed per-version
code bug can't explain the failing set changing size and membership across different runs of
similar/same Grafana versions. Panels 1-2 passing everywhere (they're dispatched/mounted first,
before concurrent load ramps up) was the other key clue.

**Actual root cause: resource contention, not a code/version bug.**
`playwright.config.ts` had `fullyParallel: true` with no `workers` cap, so ~13 test browser
contexts hit ONE shared Grafana docker container concurrently. Older/slower Grafana versions
(12.x, 13.0.x line) fall behind under that concurrent load and miss panels' 30s mount timeout
(even across 2 retries); newer versions (13.1+/nightly) are fast enough to absorb it. The
before/after job's worse failure rate is consistent with a different/heavier concurrent-load
profile in that job's own process, not a different bug. Ruled out alternative theories before
landing here: reviewed `src/dataParser.ts` (colGrouping/rowGrouping resolution, sortType switch)
and `src/matrix.js` (category header height/width/gap calcs) for an asymmetric-grouping bug --
none found, all grouping paths are symmetric and null-safe. Also ruled out a pure
scroll-position/virtualization-by-row theory via gridPos dump: panel 10 sits in the same grid row
(y=30) as failing panels 7,8,9 yet passes in the matrix job, which a row-based theory can't explain.

**Fix (pending commit): force serial execution on CI.**
`playwright.config.ts`: added `workers: process.env.CI ? 1 : undefined` right after `retries`, with
a comment explaining the contention rationale. This removes concurrent load on the shared container
entirely; the suite is small enough that serial execution costs little wall-clock time. Also
reverted the now-diagnostically-exhausted `test.beforeEach` console/pageerror instrumentation in
`tests/matrix-panel.spec.ts` (checked against both passing and all 3 failing matrix jobs -- only
ever showed irrelevant Grafana-platform noise, nothing panel-specific) and tightened the viewport
comment in `playwright.config.ts` to note it's a secondary factor, not the main fix.

**STATUS: PUSHED (commit 7a476bc) AND FALSIFIED.** Direct log verification on run 32599185877:
- `e2e test grafana-enterprise@12.4.9` (job 97095003341): IDENTICAL failure signature to before the
  fix -- still exactly 6 failed (panels 7,8,9,11,12,13), 8 passed, same `scrollIntoViewIfNeeded` 30s
  timeout, across all 3 attempts, now running fully serial (workers:1).
- `Before/after panel screenshots` (job 97094984757): still 11 failed / 3 passed, unchanged from the
  pre-fix baseline.
- 12.3.11 and 13.0.7 also failed on this run (confirmed via webhook notifications), consistent with
  12.4.9; nightly/13.1.4/13.2.0 still passed.
Forcing fully serial execution (zero concurrency) changed NOTHING in either job's failure pattern.
**Resource contention is therefore ruled out as the root cause.** The failure is deterministic and
tied to something else entirely.

**ACTUAL ROOT CAUSE FOUND: dashboard-level `preload: false`.**
`provisioning/dashboards/dashboard.json` had a top-level `"preload": false` (Grafana's default).
Per Grafana's own docs (lazy-loading-dashboards blog, dashboard JSON model docs): when `preload` is
false, panels only start their queries/mount once scrolled into view via Grafana's Scenes-based
intersection-observer lazy-load mechanism -- independent of the page's actual scroll geometry (a tall
viewport that puts everything "in view" on paper does NOT guarantee Grafana's own internal
intersection logic considers them visible early enough, especially before layout has fully settled).
This mechanism's behavior/timing clearly differs across the 13.0.7 -> 13.1.4 boundary, exactly
matching the pass/fail split (12.3.11/12.4.9/13.0.7 fail, 13.1.4/13.2.0/nightly pass). It also
explains why neither prior fix worked: viewport size doesn't affect Grafana's own intersection
threshold, and concurrency/serialization doesn't touch it either -- this is a lazy-load activation
gap in older Grafana versions, not a race or resource-starvation bug.
**Fix (this segment):** set `"preload": true` on the dashboard (dashboard.json), which makes every
panel start loading immediately regardless of scroll position, sidestepping the buggy lazy-load path
entirely. Also reverted the now-disproven `workers: process.env.CI ? 1 : undefined` in
playwright.config.ts (it cost ~5x wall-clock time -- jobs went from <2min to 9.5-9.7min -- with zero
verified benefit) and updated the viewport comment to stop citing the debunked theories. Kept the
tall 1280x2400 viewport since it's harmless and still avoids scrolling for screenshots.
**STATUS: PUSHED (commit 62dc6f3) AND CONFIRMED CORRECT.** Run 32603268435: all 6 Grafana-version
matrix jobs (12.3.11, 12.4.9, 13.0.7, 13.1.4, 13.2.0, nightly) went fully green (13/13 each) --
`preload:true` fixed the real bug completely. Confirmed via the PR's own "Playwright test results"
comment (all 6 rows ✅) and cross-checked against the check_run events.

**Remaining failure was a SEPARATE, pre-existing CI-infra bug, unrelated to preload.** The
"Before/after panel screenshots" job (pr-screenshot-diff, job 97104727261) still failed with the
same 11-failed/3-passed signature seen on every prior attempt regardless of fix (workers:1, then
preload:true) -- the tell that it was never actually caused by either theory. Root cause (found by
reading ci.yml + job log together): this job's "Run smoke specs (before)" step (ci.yml:363-364) runs
`yarn e2e` against the PR's merge-base checkout (`git checkout <merge-base sha>` at ci.yml:348) --
i.e. the OLD pre-fix dashboard.json (`preload: false`) -- specifically to capture "before" screenshots
for reviewer comparison against the "after" (PR head) run. That's working as intended: the "before"
state SHOULD fail those lazy-load-dependent panels on Grafana 12.4.0, since demonstrating the bug is
the whole point of "before". But the step had no `continue-on-error`, so a failing `yarn e2e` there
aborted the entire job before it ever reached "Capture after screenshots" / upload.
**Fix (commit pending push): added `continue-on-error: true` to the "Run smoke specs (before)" step.**
This lets the before-run's expected failures pass through without failing the job, while the "after"
run (now fixed, using PR head's `preload:true` dashboard.json) is what actually gates success.
**STATUS: NOT YET PUSHED/VERIFIED.** Next step: commit + push, poll CI directly to confirm
pr-screenshot-diff goes fully green (both before/after screenshot sets captured and uploaded,
"after" smoke specs 13/13 passing) alongside the already-confirmed-green 6-way matrix.
